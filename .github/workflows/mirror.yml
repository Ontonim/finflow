- name: Push mirror to personal repo
  env:
    PERSONAL_REPO_TOKEN: ${{ secrets.PERSONAL_REPO_TOKEN }}
  run: |
    git remote add mirror https://${{ github.actor }}:${PERSONAL_REPO_TOKEN}@github.com/arifadnan05/finflow.git
    git push mirror --all
    git push mirror --tags